<section class="section">
  <div class="container">
    <div class="columns">
      <div class="column">
        <%= link_to "New Game", new_game_path, class: "button is-primary" %>
      </div>
    </div>
    <div class="columns">
      <div class="column">
        <% @games.each do |game| %>
          <div class="tile<%= " is-active" if @game == game %> game-list-item">
            <%= link_to game_path(game) do %>
              <h3 class="game-title subtitle"><%= game.name %></h3>
              <ul class="game-params">
                <li class='rating-type'><%= game.rating_type %> – <%= game.allow_ties ? 'Ties allowed' : 'No ties' %></li>
                <li class='num-players'><%= game.players.length %> players</li>
                <li class='games-played'><%= game.results.length %> games played</li>

              </ul>

              <% result = game.recent_results.limit(1).first %>
              <% if result %>
                <div class="recent-result">
                  <h4 class="subtitle">Last game</h4>
                  <div class="description">

                    <% result.teams.each_with_index do |team, i| %>
                      <% team.players.each do |winner| %>
                        <span class="player">
                          <strong><%= winner.name %></strong>
                        </span>
                      <% end %>
                      <% if i == 0 %>
                        <% if result.tie? %>
                          ties
                        <% else %>
                          defeated
                        <% end %>
                      <% end %>
                    <% end %>
                    <br/>
                    <time><%= format_time(result.created_at) %></time>
                  </div>
                </div>
              <% end %>
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="column is-3">
        <h2 class="subtitle">Players</h2>
        <%= link_to "New Player", new_player_path, class: "button" %>

        <ul id='players-list'>
          <% @players.each do |player| %>
            <li>
              <%= link_to player_path(player) do %>
                <%= image_tag(gravatar_url(player, size: 24)) %>
                <%= player.name %>
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</section>
